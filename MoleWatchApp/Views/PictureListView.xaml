<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MoleWatchApp.ViewModels;assembly=MoleWatchApp"
             x:Class="MoleWatchApp.Views.PictureListView"
             x:Name="PictureListPage" Title="{Binding PageTitle}"
             Appearing="PictureListView_OnAppearing">


    <ContentPage.BindingContext>
        <viewModels:PictureListViewModel x:Name="PictureListViewModel" />
        
    </ContentPage.BindingContext>

    <ContentPage.Content>

        <ScrollView>
            <StackLayout x:Name="PictureStackLayout">
                <TableView x:Name="PictureListTableView" Intent="Form" >

                </TableView>
                <ActivityIndicator IsRunning="{Binding BaseIsBusy}"/>


                <ListView x:Name="HiddenPictureListView" 
                          ItemsSource="{Binding CompletePictureList}"
                          IsEnabled="{Binding PicturesFullyLoaded}"
                          PropertyChanged="HiddenPictureListView_OnPropertyChanged" 
                          ItemAppearing="HiddenPictureListView_OnItemAppearing" IsVisible="True"/>

            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>